import styled from 'styled-components';
import { Row, Image } from 'antd';

export const StyledRow = styled(Row)`
  padding: 8px 0;
`;

export const CharacterImg = styled(Image)`
  height: auto;
  width: 95%;
`;
